<section class="hero">
  <div class="hero__content">
    <h1>Elevate Your Everyday Beauty</h1>
    <p>Relax, renew, and glow with our tailored salon experiences.</p>
    <div class="hero__actions">
      <a class="btn btn--primary" href="/services">Explore Services</a>
      <a class="btn" href="/contact">Book Appointment</a>
    </div>
  </div>
</section>

<style>
  .hero { padding: 64px 24px; background: linear-gradient(120deg, var(--brand) 0%, #fff 40%, var(--brand-2) 100%); border-bottom: 1px solid var(--border); }
  .hero__content { max-width: 900px; margin: 0 auto; text-align: center; }
  .hero h1 { font-size: 2.25rem; margin: 0 0 12px; color: var(--text-strong); }
  .hero p { font-size: 1.125rem; color: var(--text); margin: 0 0 16px; }
  .hero__actions { display: flex; gap: 12px; justify-content: center; }
  .btn { display: inline-block; padding: 10px 16px; border-radius: 10px; border: 1px solid var(--border); color: var(--text-strong); text-decoration: none; background: var(--surface); }
  .btn--primary { background: var(--brand); color: var(--brand-contrast); border-color: var(--brand); }
  .btn--primary:hover { filter: brightness(0.95); }
</style>
